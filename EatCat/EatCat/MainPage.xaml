<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:EatCat"
             x:Class="EatCat.MainPage">
<StackLayout>
    <ListView ItemsSource="{Binding Alimentadores}">
        <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Orientation="Horizontal">
                            <Label Text="{Binding NomeAlimentador}" HorizontalOptions="FillAndExpand"></Label>
                            <Button Text="Alimentar" HorizontalOptions="FillAndExpand"></Button>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
    </ListView>
</StackLayout>
	

</ContentPage>